[tool.poetry]
name = "Connect4"
version = "0.1.0"
description = ""
authors = ["Chidindu Alim <chidhindh@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.8"
pygame = "^2.0.0"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
